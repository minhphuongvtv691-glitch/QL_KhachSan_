# ===================================================
# Quanlykhachsan - Server Configuration
# ===================================================
#
# AUTO-FALLBACK: Nếu server.url không kết nối được,
#               ứng dụng tự động chuyển về localhost:8081.
#               Không cần sửa file thủ công khi server remote bị tắt.
#
# --- Chế độ REMOTE (ngrok / Railway / Render / VPS / ...) ---
# Uncomment dòng dưới và thay bằng URL server thực tế của bạn:
# server.url=https://washable-martine-nonvicariously.ngrok-free.dev/api

# --- Chế độ MÁY ẢO LINUX (Dùng khi Server chạy trong Docker trên máy ảo) ---
# Đổi địa chỉ IP này thành địa chỉ IP thực tế của máy Linux (Ví dụ: 192.168.31.68)
# server.url=http://192.168.31.68:8081/api
server.url=http://192.168.56.102:8081/api
# # --- Chế độ LOCAL (Chỉ dùng khi test thử Docker trên chính máy Windows này) ---
# server.url=http://localhost:8081/api





# # --- Chế độ LOCAL (mặc định) ---
# # Nếu Backend chạy trong Docker thì dùng cổng 8081. Nếu chạy trực tiếp trên NetBeans thì đổi lại thành 8080.
# server.url=http://localhost:8081/api